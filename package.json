{
  "name": "football-sweepstake-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Portfolio-safe office football sweepstake dashboard reconstruction.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm run test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
